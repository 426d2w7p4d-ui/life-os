# Life OS v0.2.1 实现说明

这版把用户确认的 v0.2.1 网页原型正式合并回 React + TypeScript + Dexie PWA 项目。

## 本轮重点完成

- 首页新增「实时课程助手」：读取设备当前时间，自动识别正在上课 / 下一节课 / 距离上课或下课多久。
- 学习中心同步显示实时课程助手。
- 日历重构为 年 / 月 / 周 / 日 四种视图。
- 月视图显示完整 6 周网格，支持左右滑动、前后切换、点击日期进入当天。
- 周视图显示 7 天时间网格；日视图汇总课程与任务时间线。
- 课程表重构为 周课表 / 今天 / 按星期查看。
- 课程字段补全：教师、教室、星期、起止时间、开始周、结束周、单双周、考试日期、备注、颜色。
- 课程作业支持文字要求、截止日期、图片 / PDF / 文件附件，并自动进入学习日历。
- 普通任务支持说明、开始 / 结束时间和附件。
- 纪念日重构：主视觉卡、分类筛选、卡片 / 时间线 / 日历显示方式、封面、提醒天数、正计时 / 倒计时 / 同时显示、回忆相册。
- 宠物中心重构：多宠物切换、头像图标 / 照片、年龄、体重、到家天数、疫苗、狂犬、驱虫、体检、疾病、手术、用药、过敏、体重、饮食、护理、行为、自定义记录、附件、体重趋势和健康时间线。
- 工作台增强：当前客户、本期交付、制作流水线、最近视频、内容结构、客户 / 视频 / 看板入口。
- 客户档案补全：联系人、电话、平台、账号、定位、合作周期、约定视频量、状态、备注。
- 视频档案补全：选题、文案、脚本、拍摄 / 发布日期、状态、内容类型、素材备注、发布链接、播放 / 点赞数据。
- 「我的」页面增加纪念日入口。
- 修复旧版 Dexie `Table.first()` 构建错误，统一使用合法 Collection 查询。
- GitHub Pages workflow 使用 Node 22，移除无 lockfile 时容易失败的 npm cache 配置。

## 课程提醒说明

实时课程助手会一直按照设备时间自动刷新。点「提醒我」后会申请系统通知权限，并在应用处于运行状态时对“课前约 15 分钟”和“下课前约 5 分钟”做提醒。

真正做到 iPhone Web App 完全退出后仍由系统定时唤醒提醒，需要后续接入 Web Push / 服务端推送，本版没有把核心课程功能依赖在推送服务上。

## 本轮检查

在当前离线执行环境中完成：

- 42 个 TypeScript / TSX 文件语法转译检查：0 错误。
- 本地相对 import 解析检查：0 缺失。
- TypeScript 结构类型检查（使用本地离线依赖声明）：通过。
- 148 个 `<button>` 静态交互审计：全部带有 `onClick` 或明确按钮行为。
- GitHub Pages workflow 路径和部署文件已保留。

当前运行环境 DNS 无法解析 npm registry，因此这里无法执行真实 `npm install`。上传 GitHub 后，GitHub Actions 会在联网环境中执行正式 `npm install` + `npm run build`，这也是最终部署验收点。
