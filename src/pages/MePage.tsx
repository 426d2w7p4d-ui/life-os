import { useNavigate } from 'react-router-dom';
import { PawPrint, WalletCards, StickyNote, Sparkles, Search, Settings, DatabaseBackup, ChevronRight, CalendarHeart } from 'lucide-react';
import { Card, PageHeader, Section } from '../components/ui';
export function MePage(){const nav=useNavigate();const items=[[CalendarHeart,'纪念日与倒数日','生日、考试、旅行和回忆','/countdowns'],[PawPrint,'宠物中心','档案、健康记录与提醒','/pets'],[WalletCards,'记账','消费、收入与预算','/finance'],[StickyNote,'备忘录','灵感、清单与关联资料','/notes'],[Sparkles,'每日复盘','自动汇总今天的数据','/review'],[Search,'全局搜索','搜索任务、课程、客户等','/search'],[DatabaseBackup,'数据与备份','导出或导入全部数据','/settings#backup'],[Settings,'设置','日历、分类与实验功能','/settings']] as const;return <div className="page"><PageHeader title="我的" subtitle="你的数据默认只留在这台设备里。"/><Section title="生活工具"><Card>{items.map(([Icon,title,sub,path])=><button key={path} onClick={()=>nav(path)} className="list-link" style={{width:'100%',borderLeft:0,borderRight:0,borderTop:0,background:'transparent'}}><div className="row"><div className="icon-box"><Icon size={19}/></div><div style={{textAlign:'left'}}><strong>{title}</strong><div className="subtle" style={{fontSize:12}}>{sub}</div></div></div><ChevronRight size={18} color="#a2a7b0"/></button>)}</Card></Section></div>}
